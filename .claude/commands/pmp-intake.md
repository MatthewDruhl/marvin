---
description: Interactive PMP application intake — Q&A interview to gather all data needed for Matt or Emily's PMI application
---

# /pmp-intake

Run the PMP application intake interview for Matt or Emily.

## Instructions

The user has invoked: `/pmp-intake`

Arguments (if provided): `$ARGUMENTS`

Load and follow `skills/pmp-intake/SKILL.md` exactly.

- If a name was provided in `$ARGUMENTS`, use it (case-insensitive match to "matt" or "emily")
- If no name was provided, ask: "Who is this intake for — Matt or Emily?"

Start immediately. Do not summarize the plan — just begin the interview.
